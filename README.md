# 脚本使用说明

## 简介
这是一个 Bash 脚本，用于接收并输出 `apiHost`、`apiKey`、`nodeID` 三个参数。  
**所有参数必须通过命令行传入，不能为空。**

---

## 使用方法

执行脚本时需传入 3 个参数，格式如下：

```bash
./install.sh --apiHost=<地址> --apiKey=<密钥> --nodeID=<节点ID>
